
export class Whatever {

}

/*export class SnowStopProduct {
    code: string
    productInfo: Product
    type: string

    constructor(code: string, productInfo: Product, type: string) {
        this.code = code;
        this.productInfo = productInfo;
        this.type = type;
    }

}*/

/*export class Holder extends SnowStopProduct {
    resistance: number
    roofType: RoofType

    constructor(code: string, productInfo: Product, type: string, resistance: number, roofType: RoofType) {
        super(code, productInfo, type);
        this.resistance = resistance;
        this.roofType = roofType;
    }

}*/

/*export class Retainer extends SnowStopProduct {
    resistance: RetainerResistance
    profile: string

    constructor(code: string, productInfo: Product, type: string, resistance: RetainerResistance, profile: string) {
        super(code, productInfo, type);
        this.resistance = resistance;
        this.profile = profile;
    }

}*/